import { PlannedPage } from "@/components/layout/planned-page";

export default function TenantsPage() {
  return (
    <PlannedPage
      description="Tenant profiles will connect people, active leases, documents, balances, and unit history."
      message="Tenant records are planned with the lease backbone, but no tenant workflow has been added yet."
      title="Tenants"
    />
  );
}
