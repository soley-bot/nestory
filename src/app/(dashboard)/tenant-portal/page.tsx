import { PlannedPage } from "@/components/layout/planned-page";

export default function TenantPortalPage() {
  return (
    <PlannedPage
      description="The tenant portal will give renters a focused place for balances, documents, requests, and messages."
      message="Tenant portal work comes later, after tenant, lease, payment, and maintenance flows exist."
      title="Tenant portal"
    />
  );
}
