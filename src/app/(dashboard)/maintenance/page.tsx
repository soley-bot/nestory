import { PlannedPage } from "@/components/layout/planned-page";

export default function MaintenancePage() {
  return (
    <PlannedPage
      description="Tasks and maintenance will track requests, repairs, assignments, vendor notes, and completion history."
      message="Tasks and maintenance are a later operations phase after the lease and tenant records are stable."
      title="Tasks & maintenance"
    />
  );
}
