import { PlannedPage } from "@/components/layout/planned-page";

export default function WorkflowsPage() {
  return (
    <PlannedPage
      description="Workflows will automate repeatable property operations once the underlying modules are stable."
      message="Workflow automation is a future phase, with no automation engine or rules added yet."
      title="Workflows"
    />
  );
}
