import { PlannedPage } from "@/components/layout/planned-page";

export default function PaymentsPage() {
  return (
    <PlannedPage
      description="Payments will connect tenant balances, rent collection, receipts, and financial reporting."
      message="Payment workflows are planned after charges, balances, and accounting rules are defined."
      title="Payments"
    />
  );
}
