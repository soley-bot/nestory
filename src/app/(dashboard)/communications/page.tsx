import { PlannedPage } from "@/components/layout/planned-page";

export default function CommunicationsPage() {
  return (
    <PlannedPage
      description="Communications will centralize tenant, owner, and internal messages around the records they affect."
      message="Communications are planned as a later PMS phase and are not wired to messaging yet."
      title="Communications"
    />
  );
}
