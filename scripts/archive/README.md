# Archived scripts

These files are retained for provenance but are not part of the executable script inventory.

- `typescript-alias-loader.mjs` was introduced for direct Node execution of TypeScript modules before the active verification tools were moved to plain `.mjs` cores. The repository has no package command, workflow, import, or documented operator path using it as of 14 August 2026.
